{
  "name": "web-centro-psicologico",
  "private": true,
  "description": "Herramientas de build del sitio (no se usan en el navegador)",
  "scripts": {
    "build:css": "node scripts/build-css.cjs"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "purgecss": "^6.0.0"
  }
}
